Mutil.modules = Mutil.modules || {};
if(!Mutil.modules.advanced_selectors) {
    Mutil.modules.advanced_selectors = function(app) {
        if(app.Selector) {
            // require 'advanced_selectors/by_attribute'
            // require 'advanced_selectors/adjacent'
            // require 'advanced_selectors/direct_descendant'
        }
    };
}